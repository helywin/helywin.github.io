---
title: breakpad崩溃日志收集
date: 2019-09-07 20:09:47
tags:	
	- breakpad
---

## 介绍

breakpad是Google chromium项目中的一个C/C++程序崩溃日志收集工具，支持跨平台

## breakpad安装

arch linux直接通过pacman安装。

从github上克隆代码

```bash
git clone https://github.com/google/breakpad.git
```

进入代码目录进行编译

```
cd breakpad
mkdir build
cd build
../configure --prefix=/usr/local
make
sudo make install
```

编译时会报错

```
../src/client/linux/crash_generation/crash_generation_client.cc:40:10: fatal error: third_party/lss/linux_syscall_support.h: 没有那个文件或目录
 #include "third_party/lss/linux_syscall_support.h"
```

将头文件

https://raw.githubusercontent.com/adelshokhy112/linux-syscall-support/master/lss/linux_syscall_support.h

下载到 `src/third_party/lss`下，新建目录lss

返回原来目录重新执行`make`和`sudo make install`

breakpad不包含cmake库，可以通过cmake调用pkg-config来找到具体安装位置和链接库

## 测试

CMakeLists.txt

```cmake
cmake_minimum_required(VERSION 3.5)
project(test_breakpad)

set(CMAKE_CXX_COMPILER g++)
set(CMAKE_C_COMPILER gcc)

include(FindPkgConfig)
pkg_check_modules(BREAKPAD_CLIENT REQUIRED breakpad-client)
message(STATUS "breakpad_client目录 ${BREAKPAD_CLIENT_INCLUDE_DIRS}")
include_directories(${BREAKPAD_CLIENT_INCLUDE_DIRS})

set(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/output)
add_executable(test_breakpad main.cpp)
target_link_libraries(test_breakpad
        ${BREAKPAD_CLIENT_LIBRARIES}
        pthread)
```

main.cpp

```cpp
#include <breakpad/client/linux/handler/exception_handler.h>

static bool dumpCallback(const google_breakpad::MinidumpDescriptor& descriptor,
                         void* context, bool succeeded) {
    printf("Dump path: %s\n", descriptor.path());
    return succeeded;
}

void crash() { volatile int* a = (int*)(NULL); *a = 1; }

int main(int argc, char* argv[]) {
    google_breakpad::MinidumpDescriptor descriptor("./");
    google_breakpad::ExceptionHandler eh(descriptor, NULL, dumpCallback, NULL, true, -1);
    crash();
    return 0;
}
```

执行cmake时加上`-DCMAKE_BUILD_TYPE=Debug`，编译时会加上-g参数

运行编译的`test_breakpad`程序，会在同目录下生产一个dmp文件

执行如下两条语句

```bash
minidump-2-core c3d2820e-8f08-4d5f-8dfe0b8b-da5a8026.dmp > crash.core
gdb test_breakpad crash.core
```

最后可以看到如下输出

```
Core was generated by `/home/helywin/code/test_breakpad/output/test_breakpad'.
Program terminated with signal SIGSEGV, Segmentation fault.
#0  0x00005651ecd5dbc0 in crash () at /home/helywin/code/test_breakpad/main.cpp:13
13	void crash() { volatile int* a = (int*)(NULL); *a = 1; }
```

后续还将测试其他框架，以及在release编译下（关闭-g参数）时的实用性